type ServerFormValues = {
    title: string;
    ssh_user_name: string;
    address: string;
    ssh_port: number;
    ssh_key_id: string;
    project_id: string;
};

export default ServerFormValues;
