import React from 'react';
import { PipelineWithViewOptions } from '../../Pipelines';
declare const Details: ({ jobId, pipeline, }: {
    jobId: string;
    pipeline: PipelineWithViewOptions;
}) => React.JSX.Element;
export default Details;
