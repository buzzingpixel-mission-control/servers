import React, { Dispatch, SetStateAction } from 'react';
declare const AddNewKeyOverlay: ({ slug, setIsOpen, }: {
    slug: string;
    setIsOpen: Dispatch<SetStateAction<boolean>>;
}) => React.JSX.Element;
export default AddNewKeyOverlay;
